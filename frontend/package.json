{
  "name": "robot-frontend",
  "private": true,
  "scripts": {
    "dev:admin": "pnpm --filter admin dev",
    "dev:kiosk": "pnpm --filter kiosk dev",
    "dev:mapeditor": "pnpm --filter mapeditor dev",
    "dev:nurse": "pnpm --filter nurse dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}
