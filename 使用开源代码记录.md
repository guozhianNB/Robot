## mai-bot
> https://github.com/Mai-with-u/MaiBot  
用于提供人性化对话灵感
参考了
- 记忆
- 嵌入式模型
- 用户画像

## mcp-gateway（未启用）
> https://github.com/agentic-community/mcp-gateway-registry
用于桥接mcp服务，便于拓展

## ROS-SLAM-Map-Editor
> https://github.com/GyroPalm/ROS-SLAM-Map-Editor.git
用于网页地图编辑

## jie_ware
> https://github.com/6-robot/jie_ware
用于提供机器人相关的工具和资源
激光雷达定位、代价地图清除、激光雷达滤波功能（包内 `lidar_loc` / `costmap_cleaner` / `lidar_filter_node`）

- 定版 `21bf8f5`（2026-09-19），许可 **GPL-2.0-or-later**（copyleft，注意传染性；本仓库其它外部工程走 `.gitignore` 不纳管，本包是 **vendoring 进主仓库**的例外）
- **已 vendored 进主仓库**（`ros2_car/src/jie_ware/`，克隆后删掉内层 `.git`）：主仓库只保存源码快照，不保留上游历史，故在此钉住上游 commit，将来同步上游要按此哈希做 diff
- ⚠️ **它是 ROS1（catkin）包，不是 ROS2 包**（`find_package(catkin)` / `#include <ros/ros.h>` /
  `<buildtool_depend>catkin</buildtool_depend>`），在本车的 ROS2 Humble 上既不能 `colcon build`
  也不能直接运行；目录下已放 `COLCON_IGNORE` 把它排除出构建。
- **本包只作参考，未抄入任何源码**。三个功能由 `ros2_car` 按算法**清洁重写**为自研代码
  （与仓库其余部分同为 MIT，不引入 GPL 传染）：
  `robot_bringup/robot_bringup/scan_filter.py`（对应 `lidar_filter_node`）、
  `robot_navigation/robot_navigation/costmap_cleaner.py`（对应 `costmap_cleaner`）、
  `robot_bringup/robot_bringup/lidar_loc.py`（对应 `lidar_loc`）。
  改写口径与偏差见 `docs/log.md` 2026-09-19「jie_ware 三件套落进 ros2_car」。